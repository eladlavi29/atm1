.global _start

.section .text

  mov head(%rip), %rax
  cmpl $5, (%rax)
  jne bad_exit
  
  mov 4(%rax), %rax
  cmpl $1, (%rax)
  jne bad_exit
  
  mov 4(%rax), %rax
  cmpl $3, (%rax)
  jne bad_exit
  
  mov 4(%rax), %rax
  cmpl $4, (%rax)
  jne bad_exit
  
  mov 4(%rax), %rax
  cmpl $1, (%rax)
  jne bad_exit
  
  movq $60, %rax
  movq $0, %rdi
  syscall

bad_exit:
  movq $60, %rax
  movq $1, %rdi
  syscall

.section .data
E: .int 5
.quad 0

D: .int 4
.quad E

C: .int 3
.quad D

B: .int 1
.quad C

A: .int 1
.quad B

head: .quad A

Source: .quad 5

Value: .int 1